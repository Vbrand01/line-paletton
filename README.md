# Paletton

Explorador de paletas de cores.

## Executar localmente

Pré-requisitos: Node.js

1. Instalar dependências: `npm install`
2. Rodar em desenvolvimento: `npm run dev`
3. Gerar build de produção: `npm run build`
4. Visualizar build: `npm run preview`
